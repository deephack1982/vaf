<%= form_for(@squadron) do |f| %>
  <% if @squadron.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@squadron.errors.count, "error") %> prohibited this squadron from being saved:</h2>

      <ul>
      <% @squadron.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.select :aircraft, Squadron::AIRCRAFT, {:include_blank => 'Select...'} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
