<%= form_for(@airfield) do |f| %>
  <% if @airfield.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@airfield.errors.count, "error") %> prohibited this airfield from being saved:</h2>

      <ul>
      <% @airfield.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.select :airfield_type, Airfield::AIRFIELD_TYPE %>
  </div>
  <div class="field">
    <%= f.number_field :ATC_channel, :step => 'any' %>
  </div>
  <div class="field">
    <%= f.number_field :ATIS_channel, :step => 'any' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
