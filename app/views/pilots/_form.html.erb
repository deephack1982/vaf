<%= form_for(@pilot) do |f| %>
  <% if @pilot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pilot.errors.count, "error") %> prohibited this pilot from being saved:</h2>

      <ul>
      <% @pilot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <div class="large-6 columns">
      <div class="field">
        <%= f.text_field :name %>
      </div>
      <div class="field">
        <%= f.text_field :email %>
      </div>
    </div>
    <div class="large-6 columns">
      <div class="field">
        <%= f.text_field :callsign %>
      </div>
      <div class="field">
        <%= f.label :country %>
        <%= f.country_select :country %>
      </div>
    </div>
  <div class="field">
      <%= f.password_field :password %>
  </div>
  <div class="field">
    <%= f.password_field :password_confirmation %>
  </div>
  <div class="field">
    <%= f.check_box :admin %>
  </div>
  <div class="field">
    <%= f.select :rank, Pilot::PILOT_RANK %>
  </div>
  <div class="field">
    <%= f.collection_select :squadron_id, Squadron.all, :id, :name, {:include_blank => "Select..."} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
